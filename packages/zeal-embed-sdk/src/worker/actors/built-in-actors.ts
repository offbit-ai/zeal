/**
 * Built-in actors for the worker runtime
 */

import { WorkerActorFactory } from './worker-actor';

/**
 * Register all built-in actors
 * For now, this is a placeholder - actors can be added as needed
 */
export function registerBuiltInActors(): void {
  // Register built-in actors here
  // Example:
  // WorkerActorFactory.register('http-request', HttpRequestActor);
  // WorkerActorFactory.register('data-transform', DataTransformActor);
  
  // For now, no built-in actors are registered
  // They can be added as the system develops
}